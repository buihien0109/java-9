// #739ba8, #828abc
function randomHexColor() {
    // Tạo 1 mảng bao gồm 16 ký tự : [0-9][a-f]

    // Sử dụng vòng lặp 6 lần
    // Mỗi vòng lặp random ra 1 ký tự trong mảng

    // Trả về mã hex
}

// rgb(34,13,123), rgb(52,145,213)
function randomRgbColor() {
    // Random 3 giá trị r,g,b
    // Mỗi giá trị từ 0 -> 255

    // Cộng chuỗi lại để có mã màu rgb
}